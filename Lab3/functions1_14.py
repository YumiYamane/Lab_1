'''
#4 filter() is a built-in function that allows you to process an iterable and extract those items that satisfy a given condition.

list(filter(lambda x: is_prime(x), numbers))

#5 Permutations means different orders by which elements can be arranged.

from itertools import permutations 
a = permutations(s)

#6 The split() function in Python is a built-in string method that is used to split a string into a list of substrings based on a specified delimiter. 
   The reverse() function modifies the original list by reversing the order of its elements.

a = s.split()
a.reverse()

#8 The pop() function removes an element from the specified index.
 
list.pop(0)

#10 The append() function of Python is used to add an item at the end of the list to which it is applied.
 
unique.append(i)

#13 randint() is a built-in function of the random module in Python that returns a random integer between the higher and lower limit passed as parameters. 

import random
secret_num = random.randint(1, 20)

'''